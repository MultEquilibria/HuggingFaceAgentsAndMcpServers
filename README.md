This library contains a host of HuggingFace agents implementations along with some simple MCP servers implementations 

It has both agent script implementations and implementation that are based on the configuration only that are instantiated by Tiny Agents library provided by Hugging Face 